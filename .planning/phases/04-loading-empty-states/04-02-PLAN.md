---
phase: 04-loading-empty-states
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - railway_apps/aiaa_dashboard/templates/execution_history.html
  - railway_apps/aiaa_dashboard/templates/dashboard_v2.html
  - railway_apps/aiaa_dashboard/templates/components/empty_state.html
autonomous: true

must_haves:
  truths:
    - "Execution history page shows 'Run your first skill' CTA button when no executions exist"
    - "Client list page shows 'Add your first client' CTA button when no clients exist"
    - "Dashboard recent activity shows 'No activity yet' with guidance when empty"
    - "Dashboard search results show 'No skills match -- try different keywords' when no search matches"
  artifacts:
    - path: "railway_apps/aiaa_dashboard/templates/execution_history.html"
      provides: "Enhanced empty state with 'Run your first skill' CTA linking to /skills"
      contains: "Run your first skill"
    - path: "railway_apps/aiaa_dashboard/templates/dashboard_v2.html"
      provides: "Enhanced empty states for recent activity and search results"
      contains: "No skills match"
    - path: "railway_apps/aiaa_dashboard/templates/components/empty_state.html"
      provides: "Reusable empty state macro with skill icon type"
      contains: "skill"
  key_links:
    - from: "execution_history.html empty state"
      to: "/skills"
      via: "CTA button href"
      pattern: "Run your first skill"
    - from: "dashboard_v2.html search results"
      to: "empty search message"
      via: "JS renders message when matches.length === 0"
      pattern: "No skills match"
---

<objective>
Add meaningful empty states with actionable CTAs to all list/history pages and the dashboard so users always know what to do next when views have no data.

Purpose: Satisfies UX-02 (execution history CTA), UX-03 (client list CTA -- verify existing), UX-05 (dashboard activity guidance), and UX-06 (search results empty message).
Output: Enhanced empty states in execution_history.html, dashboard_v2.html, and the empty_state.html component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@railway_apps/aiaa_dashboard/templates/execution_history.html
@railway_apps/aiaa_dashboard/templates/dashboard_v2.html
@railway_apps/aiaa_dashboard/templates/clients.html
@railway_apps/aiaa_dashboard/templates/components/empty_state.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance empty states in execution history, dashboard activity, and search results</name>
  <files>railway_apps/aiaa_dashboard/templates/execution_history.html, railway_apps/aiaa_dashboard/templates/dashboard_v2.html, railway_apps/aiaa_dashboard/templates/components/empty_state.html</files>
  <action>
  **empty_state.html changes:**

  1. Add a new icon type `skill` to the Jinja2 macro, using the lightning bolt SVG (polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2") -- the same icon used throughout the app for skills. Add it as an `{% elif icon == 'skill' %}` block after the existing icon blocks.

  **execution_history.html changes:**

  1. Find the existing empty state block (lines ~372-375):
     ```
     {% from "components/empty_state.html" import empty_state %}
     {{ empty_state('execution', 'No executions yet', 'Execution history will appear here once workflows run.') }}
     ```
  2. Replace with an enhanced version that includes a CTA:
     ```
     {% from "components/empty_state.html" import empty_state %}
     {{ empty_state('skill', 'No executions yet', 'Run your first skill to see execution history here.', '/skills', 'Run Your First Skill') }}
     ```
     This uses the skill icon and provides an action_url ('/skills') with action_text ('Run Your First Skill').

  **dashboard_v2.html changes:**

  1. Find the empty-hint div in the Recent Activity section (around line 465):
     ```html
     <div class="empty-hint">No recent activity yet. Run a skill to get started!</div>
     ```
     Replace with a more structured empty state:
     ```html
     <div class="empty-hint" id="activity-empty-state">
         <div style="margin-bottom: 0.5rem;">
             <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="color: var(--text-muted);">
                 <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
             </svg>
         </div>
         <div style="font-weight: 500; color: var(--text-primary); margin-bottom: 0.25rem;">No activity yet</div>
         <div>Your recent skill executions will appear here.</div>
         <a href="/skills" class="btn" style="margin-top: 0.75rem; font-size: 0.75rem; padding: 0.5rem 1rem;">Run a Skill</a>
     </div>
     ```

  2. In the loadRecentActivity() JS function, update the early return for empty executions (around line 518):
     Change `if (executions.length === 0) return;` to leave the empty state visible (it is already the default content). The existing code already clears activityList.innerHTML when there ARE executions, which removes the empty state. No change needed here -- verify the existing logic is correct.

  3. Find the search results empty message in the JS (around line 607):
     ```javascript
     searchResults.innerHTML = '<div style="padding: 1rem; color: var(--text-muted); font-size: 0.875rem; text-align: center;">No matching skills found</div>';
     ```
     Replace with:
     ```javascript
     searchResults.innerHTML = '<div style="padding: 1.25rem; text-align: center;"><div style="color: var(--text-muted); font-size: 0.875rem; margin-bottom: 0.25rem;">No skills match your search</div><div style="color: var(--text-muted); font-size: 0.75rem;">Try different keywords or <a href="/workflows" style="color: var(--accent);">browse all skills</a></div></div>';
     ```

  **Verify clients.html UX-03 (no changes needed):**
  The clients.html already has a proper empty state at line 306-313 with:
  - Icon, "No clients yet" heading, description
  - "Add Your First Client" CTA button that calls openClientForm()
  This already satisfies UX-03. Just verify it exists -- do NOT modify clients.html.

  Preserve ALL existing markup and JS. Only modify the specific sections described above.
  </action>
  <verify>
  1. Load /executions with no execution data -- should show "No executions yet" with "Run Your First Skill" button linking to /skills.
  2. Load /home dashboard with no activity -- should show "No activity yet" with "Run a Skill" button.
  3. Type a non-matching search term in dashboard search -- should show "No skills match your search" with "browse all skills" link.
  4. Load /clients-manage with no clients -- should show existing "Add Your First Client" CTA (pre-existing).
  5. No JavaScript console errors on any page.
  </verify>
  <done>
  - Execution history shows "Run Your First Skill" CTA linking to /skills when empty (UX-02)
  - Client list shows "Add Your First Client" CTA when empty (UX-03 -- pre-existing, verified)
  - Dashboard recent activity shows "No activity yet" with guidance and CTA (UX-05)
  - Search results show "No skills match your search -- try different keywords" with browse link (UX-06)
  </done>
</task>

</tasks>

<verification>
1. All 4 empty state locations render correctly with appropriate icons, messages, and CTAs.
2. CTA buttons/links navigate to correct destinations (/skills, /workflows, openClientForm()).
3. Empty states are visually consistent with the existing design system (using CSS variables).
4. When data IS present, empty states do not appear (hidden by existing JS logic).
5. No console errors on any affected page.
</verification>

<success_criteria>
- UX-02: Execution history page shows "Run your first skill" CTA when empty
- UX-03: Client list page shows "Add your first client" CTA when empty (pre-existing, verified)
- UX-05: Dashboard recent activity shows "No activity yet" guidance when empty
- UX-06: Search results display "No skills match -- try different keywords" when empty
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-empty-states/04-02-SUMMARY.md`
</output>
