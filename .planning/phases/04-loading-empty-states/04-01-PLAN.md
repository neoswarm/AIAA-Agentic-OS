---
phase: 04-loading-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - railway_apps/aiaa_dashboard/static/css/v2.css
  - railway_apps/aiaa_dashboard/templates/dashboard_v2.html
  - railway_apps/aiaa_dashboard/templates/workflow_catalog.html
autonomous: true

must_haves:
  truths:
    - "User sees animated skeleton cards in the dashboard skill area while skills API loads"
    - "User sees skeleton placeholder cards in the workflow catalog during initial render"
    - "Skeletons disappear and real content appears once data arrives"
  artifacts:
    - path: "railway_apps/aiaa_dashboard/static/css/v2.css"
      provides: "Skeleton card CSS with pulse animation"
      contains: "skeleton-card"
    - path: "railway_apps/aiaa_dashboard/templates/dashboard_v2.html"
      provides: "Skeleton placeholders for quick-start grid and category chips"
      contains: "skeleton-card"
    - path: "railway_apps/aiaa_dashboard/templates/workflow_catalog.html"
      provides: "Skeleton grid shown before server-rendered cards paint"
      contains: "skeleton-card"
  key_links:
    - from: "dashboard_v2.html JS loadSkills()"
      to: "skeleton-card elements"
      via: "JS hides skeletons and shows real content after fetch resolves"
      pattern: "skeleton.*display|style.*none|classList.*remove"
---

<objective>
Add animated skeleton loading placeholders to the skill catalog and dashboard views so users see visual feedback while data loads instead of blank space.

Purpose: Satisfies UX-01 (skeleton cards while data loads). Users perceive the page as fast and responsive even before API data arrives.
Output: CSS skeleton animation classes in v2.css, skeleton placeholder markup in dashboard_v2.html and workflow_catalog.html.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@railway_apps/aiaa_dashboard/static/css/v2.css
@railway_apps/aiaa_dashboard/templates/dashboard_v2.html
@railway_apps/aiaa_dashboard/templates/workflow_catalog.html
@railway_apps/aiaa_dashboard/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton CSS classes with pulse animation to v2.css</name>
  <files>railway_apps/aiaa_dashboard/static/css/v2.css</files>
  <action>
  Append the following CSS classes to the END of v2.css (after all existing styles):

  1. `.skeleton-card` -- A rectangular placeholder matching the dimensions of `.quick-start-card` (padding 1.25rem 1rem, border-radius 12px, background var(--bg-surface), border 1px solid var(--border-subtle)). Height ~100px.

  2. `.skeleton-pulse` -- Animated shimmer effect using a keyframe animation:
     - background: linear-gradient(90deg, var(--bg-surface) 25%, var(--bg-elevated) 50%, var(--bg-surface) 75%)
     - background-size: 200% 100%
     - animation: skeleton-pulse 1.5s ease-in-out infinite

  3. `@keyframes skeleton-pulse` -- Moves background-position from 200% 0 to -200% 0.

  4. `.skeleton-line` -- A rounded rectangle for text placeholders (height 12px, border-radius 4px, background with the pulse animation). Add `.skeleton-line--short` (width 60%) and `.skeleton-line--long` (width 90%).

  5. `.skeleton-circle` -- A circle placeholder (width/height 44px, border-radius 12px for icon placeholders).

  6. `.skeleton-chip` -- Matches `.category-chip` dimensions (height ~32px, width ~80px, border-radius 9999px).

  7. `.skeleton-grid` -- Matches `.quick-start-grid` layout (display grid, same grid-template-columns and gap).

  8. `.skeleton-workflows-grid` -- Matches `.workflows-grid` layout (display grid, repeat(auto-fill, minmax(320px, 1fr)), gap 1rem).

  9. `.skeleton-workflow-card` -- Matches `.workflow-card` dimensions (background var(--bg-elevated), border 1px solid var(--border-subtle), border-radius 10px, padding 1.25rem, height ~160px).

  Do NOT use any framework or library. Pure CSS only. Use the existing CSS custom properties (--bg-surface, --bg-elevated, --border-subtle, --border, --text-muted) for theme compatibility.
  </action>
  <verify>Open v2.css and confirm skeleton classes exist with animation keyframes. No syntax errors.</verify>
  <done>v2.css contains .skeleton-card, .skeleton-pulse, .skeleton-line, .skeleton-circle, .skeleton-chip, .skeleton-workflows-grid, .skeleton-workflow-card classes and @keyframes skeleton-pulse.</done>
</task>

<task type="auto">
  <name>Task 2: Add skeleton placeholders to dashboard and catalog templates</name>
  <files>railway_apps/aiaa_dashboard/templates/dashboard_v2.html, railway_apps/aiaa_dashboard/templates/workflow_catalog.html</files>
  <action>
  **dashboard_v2.html changes:**

  1. In the quick-start-grid section, add a sibling div with id `quick-start-skeleton` containing 6 skeleton cards (matching the 6 existing quick-start-card items). Each skeleton card: `<div class="skeleton-card skeleton-pulse"><div class="skeleton-circle" style="margin: 0 auto 0.625rem;"></div><div class="skeleton-line skeleton-line--short" style="margin: 0 auto;"></div></div>`. Set `style="display:none"` on the real `#quick-start-grid` initially.

  2. In the category-chips section, add a sibling div with id `category-skeleton` containing 5 skeleton chips: `<div class="skeleton-chip skeleton-pulse"></div>`. Set `style="display:none"` on the real `#category-chips` initially.

  3. Update the loadSkills() function in the `<script>` block:
     - At the START of loadSkills(), the skeletons are visible (they're shown by default since real content is hidden).
     - After `allSkills` is populated and `renderCategories()` completes:
       - Hide `#quick-start-skeleton` (style.display = 'none')
       - Show `#quick-start-grid` (style.display = 'grid')
       - Hide `#category-skeleton` (style.display = 'none')
       - Show `#category-chips` (style.display = 'flex')
     - In the catch block, also hide skeletons and show real containers (so error state is visible, not eternal skeleton).

  **workflow_catalog.html changes:**

  1. Add a div with id `catalog-skeleton` just BEFORE `#workflows-grid`. It should contain 6 `<div class="skeleton-workflow-card skeleton-pulse"><div class="skeleton-line skeleton-line--long" style="margin-bottom:0.75rem"></div><div class="skeleton-line skeleton-line--short"></div></div>` in a `skeleton-workflows-grid` wrapper.

  2. Add a `<script>` snippet at the bottom (inside the existing script block) that hides `#catalog-skeleton` after DOM content is loaded. Since workflow_catalog.html is server-rendered via Jinja2 (the cards are in the HTML already), the skeleton only briefly flashes during network load of the page itself. Use: `document.addEventListener('DOMContentLoaded', function() { var skel = document.getElementById('catalog-skeleton'); if (skel) skel.style.display = 'none'; });`

  Preserve ALL existing markup and JS. Only ADD new elements and modify the loadSkills() function to toggle visibility.
  </action>
  <verify>
  Load dashboard_v2.html in browser -- skeleton cards should briefly appear, then be replaced by real content.
  Check workflow_catalog.html renders without JS errors.
  Inspect DOM to confirm skeleton elements exist with correct classes.
  </verify>
  <done>Dashboard shows animated skeleton placeholders in quick-start and category areas while skills API loads, then swaps to real content. Workflow catalog has skeleton grid that hides after page renders.</done>
</task>

</tasks>

<verification>
1. Load the dashboard (/home) in a browser and confirm skeleton cards animate while the skills API loads.
2. After data loads, skeletons disappear and real quick-start cards + category chips appear.
3. Load the workflow catalog (/workflows) and confirm skeleton grid appears briefly then hides.
4. No JavaScript console errors on either page.
5. Both dark and light themes render skeleton colors correctly (using CSS custom properties).
</verification>

<success_criteria>
- Skeleton cards with pulse animation appear on dashboard while /api/v2/skills loads (UX-01 satisfied)
- Skeleton cards appear in workflow catalog during initial page paint
- Skeletons cleanly transition to real content (no flicker, no layout shift)
- All CSS uses existing theme variables (works in both light and dark mode)
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-empty-states/04-01-SUMMARY.md`
</output>
