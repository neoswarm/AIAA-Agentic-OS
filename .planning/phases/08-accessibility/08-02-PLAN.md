---
phase: 08-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - railway_apps/aiaa_dashboard/static/css/main.css
  - railway_apps/aiaa_dashboard/static/css/v2.css
  - railway_apps/aiaa_dashboard/templates/login.html
  - railway_apps/aiaa_dashboard/templates/dashboard_v2.html
  - railway_apps/aiaa_dashboard/templates/skill_execute.html
  - railway_apps/aiaa_dashboard/templates/onboarding.html
  - railway_apps/aiaa_dashboard/templates/help.html
  - railway_apps/aiaa_dashboard/templates/execution_history.html
  - railway_apps/aiaa_dashboard/templates/settings.html
  - railway_apps/aiaa_dashboard/templates/env.html
  - railway_apps/aiaa_dashboard/templates/setup.html
  - railway_apps/aiaa_dashboard/templates/workflow_catalog.html
  - railway_apps/aiaa_dashboard/templates/base.html
  - railway_apps/aiaa_dashboard/static/js/skill_execution.js
autonomous: true

must_haves:
  truths:
    - "All text meets WCAG AA contrast ratio (4.5:1 normal text, 3:1 large text) in both dark and light themes"
    - "Keyboard users see a visible focus indicator on every interactive element"
    - "Tab key moves focus through all interactive elements in logical visual order on every page"
    - "Enter key submits forms and activates buttons; Escape closes modals and dropdowns"
    - "Arrow keys navigate within tab groups (settings tabs, skill mode tabs)"
    - "Skip-to-content link is visible on focus and functional"
  artifacts:
    - path: "railway_apps/aiaa_dashboard/static/css/main.css"
      provides: "WCAG AA color contrast fixes, focus-visible styles, skip-link styles"
      contains: "focus-visible"
    - path: "railway_apps/aiaa_dashboard/static/css/v2.css"
      provides: "Dashboard-specific contrast fixes and focus styles"
      contains: "focus-visible"
  key_links:
    - from: "railway_apps/aiaa_dashboard/static/css/main.css"
      to: "all templates"
      via: "CSS custom properties (--text-muted, --accent, --warning)"
      pattern: "--text-muted.*#(8a8a8a|666666)"
    - from: "railway_apps/aiaa_dashboard/static/css/main.css"
      to: "all interactive elements"
      via: "global :focus-visible rule"
      pattern: ":focus-visible"
---

<objective>
Fix all WCAG AA color contrast failures and add full keyboard navigation support across the dashboard.

Purpose: Satisfy A11Y-03 (color contrast meets WCAG AA in both themes) and A11Y-04 (all forms fully keyboard-navigable) so that low-vision users can read all content and keyboard-only users can operate every feature.

Output: CSS custom properties updated for AA-compliant contrast, :focus-visible styles on all interactive elements, skip-link styled and functional, keyboard arrow navigation for tab groups, all inline outline:none replaced with focus-visible alternatives, Enter/Escape keyboard handling on all interactive patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-accessibility/08-RESEARCH.md
@.planning/phases/08-accessibility/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color contrast fixes and focus-visible styles in CSS</name>
  <files>
    railway_apps/aiaa_dashboard/static/css/main.css
    railway_apps/aiaa_dashboard/static/css/v2.css
    railway_apps/aiaa_dashboard/templates/base.html
  </files>
  <action>
**main.css -- Color contrast fixes (CSS custom properties):**

1. In the dark theme (`:root`) update these custom properties:
   - `--text-muted`: Change from `#737373` to `#8a8a8a` (gives 4.58:1 on #1a1a1a background)
   - `--accent`: Change from `#e07a3a` to `#c06520` (gives ~4.5:1 for white text on accent buttons)

2. In the light theme (`[data-theme="light"]`) update:
   - `--text-muted`: Change from `#737373` to `#666666` (gives 5.74:1 on #ffffff)
   - `--accent`: Change from `#d96c2c` to `#a04d15` (gives ~6.5:1 for white text on accent buttons)
   - `--warning`: Change from `#ca8a04` to `#92600a` (gives ~4.5:1 on white)

3. For accent buttons specifically: if `.btn` uses `color: white` (or #fff), verify the darkened accent values provide at least 4.5:1 contrast with white text. If any combination still fails, switch button text to `#1a1a1a` (dark text) for that theme.

**main.css -- Focus-visible styles:**

4. Add a global `:focus-visible` rule (after the existing reset/base styles):
   ```css
   :focus-visible {
       outline: 2px solid var(--accent);
       outline-offset: 2px;
   }
   ```

5. For form inputs (`.form-input:focus`, `.form-select:focus`, `.form-textarea:focus`): keep the existing `outline: none` + `border-color` + `box-shadow` for mouse focus, BUT add companion `:focus-visible` rules that show the outline:
   ```css
   .form-input:focus-visible,
   .form-select:focus-visible,
   .form-textarea:focus-visible {
       outline: 2px solid var(--accent);
       outline-offset: -1px;
   }
   ```

6. Add skip-link CSS (styles for the skip-to-content link added in Plan 08-01):
   ```css
   .skip-link {
       position: absolute;
       top: -40px;
       left: 0;
       background: var(--accent);
       color: white;
       padding: 0.5rem 1rem;
       z-index: 10000;
       font-size: 0.875rem;
       font-weight: 500;
       border-radius: 0 0 8px 0;
       transition: top 0.2s;
       text-decoration: none;
   }
   .skip-link:focus {
       top: 0;
   }
   ```

7. Ensure the theme-toggle `<button>` (converted from div in Plan 08-01) has no visual difference from before -- if needed add:
   ```css
   button.theme-toggle {
       background: none;
       border: none;
       cursor: pointer;
       /* preserve existing theme-toggle styles */
   }
   ```
   Same for `button.nav-label-advanced` -- strip default button styles.

**v2.css -- Dashboard-specific fixes:**

8. Fix `.search-bar__input:focus` -- replace `outline: none` with a `:focus-visible` companion that shows outline

9. If v2.css overrides any of the colors being fixed (accent, muted, warning), update those too

**base.html (minimal):**

10. If base.html has any inline `style="outline:none"` or similar, remove them -- they should now use the CSS classes

**Login page contrast:**
11. In login.html: the login page uses its own CSS variables (in a `<style>` block). Update the login-specific `--primary` (#6366f1 on dark bg #141414 = 4.12:1, barely fails for normal text) to a lighter value like `#818cf8` (~5.5:1 on #141414). Update login-specific `--text-muted` from #737373 to `#8a8a8a` (matching main dark theme fix).
  </action>
  <verify>
    - Grep for "#8a8a8a" in main.css (dark theme muted text fix)
    - Grep for "#666666" in main.css (light theme muted text fix)
    - Grep for "#92600a" in main.css (light theme warning fix)
    - Grep for "focus-visible" in main.css (should find global rule + form input rules)
    - Grep for "skip-link" in main.css (should find the skip link styles)
    - Grep for "focus-visible" in v2.css (should find search input rule)
    - Verify login.html updated contrast values
    - Run `python3 railway_apps/aiaa_dashboard/test_app.py` to verify no rendering errors
  </verify>
  <done>
    - All WCAG AA contrast failures from the research audit are fixed in both themes
    - --text-muted, --accent, --warning custom properties updated to pass AA
    - Global :focus-visible style exists for all interactive elements
    - Form inputs have :focus-visible outline (not just border-color change)
    - Skip-link is styled (invisible until focused, then visible at top of page)
    - Button conversions (theme-toggle, nav-label-advanced) have no visual regression
    - Login page contrast failures fixed
  </done>
</task>

<task type="auto">
  <name>Task 2: Keyboard navigation for tabs, accordions, search, and inline outline:none removal</name>
  <files>
    railway_apps/aiaa_dashboard/templates/skill_execute.html
    railway_apps/aiaa_dashboard/templates/settings.html
    railway_apps/aiaa_dashboard/templates/help.html
    railway_apps/aiaa_dashboard/templates/dashboard_v2.html
    railway_apps/aiaa_dashboard/templates/execution_history.html
    railway_apps/aiaa_dashboard/templates/onboarding.html
    railway_apps/aiaa_dashboard/templates/env.html
    railway_apps/aiaa_dashboard/templates/setup.html
    railway_apps/aiaa_dashboard/templates/workflow_catalog.html
    railway_apps/aiaa_dashboard/static/js/skill_execution.js
  </files>
  <action>
**Keyboard arrow navigation for tab groups:**

1. In skill_execute.html: Add keyboard handler to the mode tabs (`role="tablist"` from Plan 08-01). When focus is on a tab, Left/Right arrow keys should move focus to previous/next tab and activate it (call switchMode). This follows W3C APG tabs pattern. Add an inline `<script>` block or extend the existing script:
   ```javascript
   document.querySelector('[role="tablist"]')?.addEventListener('keydown', function(e) {
       const tabs = Array.from(this.querySelectorAll('[role="tab"]'));
       const index = tabs.indexOf(document.activeElement);
       if (index === -1) return;
       let newIndex;
       if (e.key === 'ArrowRight') newIndex = (index + 1) % tabs.length;
       else if (e.key === 'ArrowLeft') newIndex = (index - 1 + tabs.length) % tabs.length;
       else return;
       e.preventDefault();
       tabs[newIndex].focus();
       tabs[newIndex].click();
   });
   ```

2. In settings.html: Add the same arrow key navigation for the settings tabs (`role="tablist"`).

**Keyboard navigation for FAQ accordions:**

3. In help.html: The FAQ buttons already toggle with click (and got aria-expanded in Plan 08-01). Add Enter/Space handling if not already present (they should work natively since they are `<button>` elements). Verify this is the case. If any FAQ toggle uses a non-button element, convert it.

**Keyboard navigation for search combobox:**

4. In dashboard_v2.html: Add keyboard support to the search combobox pattern (ARIA added in Plan 08-01):
   - Arrow Down from search input should focus the first result in the dropdown
   - Arrow Up/Down within results should move focus between results
   - Enter on a result should navigate to that skill
   - Escape should close the dropdown and return focus to the input
   - Update `aria-expanded` on the combobox wrapper as the dropdown opens/closes
   Add this as an inline `<script>` block or extend existing search JS.

**Keyboard navigation for expandable rows:**

5. In execution_history.html: The expandable rows (with aria-expanded from Plan 08-01) should respond to Enter/Space key when focused. If they use `onclick` on a `<tr>` or `<div>`, add `tabindex="0"` and a keydown handler for Enter/Space that triggers the same toggle function. Also add `role="button"` if the clickable element is not already a button.

**Dynamic form field keyboard accessibility:**

6. In skill_execution.js: Audit the `loadSkillForm` or equivalent function that dynamically generates form fields. Ensure:
   - Each generated input has a unique `id`
   - Each generated label has `for` matching that `id`
   - Generated error containers have `aria-live="polite"`
   - Tab order flows logically through generated fields (no need for explicit tabindex if they are standard form elements)
   This is the "open question" from the research -- resolve it now.

**Inline outline:none removal in templates:**

7. The research found 15 instances of inline `style="...outline:none..."` across these templates: login.html, skill_execute.html, onboarding.html, help.html, events.html, webhooks.html, workflow_catalog.html, env.html, setup.html, dashboard_v2.html, execution_history.html. For each:
   - Remove `outline: none` or `outline: 0` from inline styles
   - The global `:focus-visible` rule from Task 1 will handle focus indication
   - If the inline style had other properties besides outline, keep those other properties and only remove the outline part
   - Do NOT remove `outline: none` from the CSS files (main.css, v2.css) -- those were already paired with :focus-visible in Task 1

**Form enter-to-submit verification:**

8. Verify all forms have either an explicit `<button type="submit">` or an `<input type="submit">` so that Enter key submits them. This should already work via HTML defaults but verify that no form has `onkeydown` handlers preventing Enter submission.
  </action>
  <verify>
    - Grep for "ArrowRight" in skill_execute.html and settings.html (should find arrow key handlers)
    - Grep for "ArrowDown" in dashboard_v2.html (should find search combobox keyboard handler)
    - Grep for "outline.*none" or "outline.*0" in templates (inline styles) -- should find zero instances in template files (only in CSS files where they are paired with :focus-visible)
    - Grep for "aria-live" in skill_execution.js (should find on error containers)
    - Run `python3 railway_apps/aiaa_dashboard/test_app.py` to verify no rendering errors
  </verify>
  <done>
    - Tab groups (skill mode tabs, settings tabs) support Left/Right arrow key navigation
    - Search combobox supports Arrow Down/Up navigation, Enter to select, Escape to close
    - Expandable rows in execution history are keyboard-accessible
    - All inline outline:none removed from templates (15 instances)
    - Dynamic form fields (skill_execution.js) have proper id/for associations and aria-live on errors
    - All forms submit with Enter key
    - No inline focus-blocking styles remain in any template
  </done>
</task>

</tasks>

<verification>
1. Color contrast: Verify all updated CSS custom property values match the AA-compliant values from research recommendations
2. Focus indicators: Tab through every page -- every focusable element should show a visible outline
3. Keyboard navigation: Arrow keys work in tab groups, search combobox navigable by keyboard
4. Skip link: Tab once on any page -- skip-to-content link becomes visible and functional
5. Inline outlines: No template file contains inline `outline: none` or `outline: 0`
6. No regressions: `python3 railway_apps/aiaa_dashboard/test_app.py` passes
</verification>

<success_criteria>
- A11Y-03 (color contrast): All text/UI component combinations pass WCAG AA (4.5:1 normal, 3:1 large) in both dark and light themes
- A11Y-04 (keyboard navigation): All forms navigable via Tab, submittable via Enter, escapable via Escape; tab groups support arrow keys; search combobox fully keyboard-operable
- Zero inline outline:none remaining in templates
- Visible focus indicators on all interactive elements via :focus-visible
- Skip-to-content link visible and functional on keyboard focus
</success_criteria>

<output>
After completion, create `.planning/phases/08-accessibility/08-02-SUMMARY.md`
</output>
