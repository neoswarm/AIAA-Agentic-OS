---
phase: 01-regression-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-regression-baseline/BASELINE-REPORT.md
autonomous: true

must_haves:
  truths:
    - "All 7 existing tests in test_app.py pass without modification"
    - "Every dashboard page route returns HTTP 200 when authenticated"
    - "Every public route (/health, /login, /onboarding, /api/health) returns 200 without auth"
    - "All API v2 GET endpoints return valid JSON with status field"
    - "A baseline report exists documenting the current state of all routes and tests"
  artifacts:
    - path: ".planning/phases/01-regression-baseline/BASELINE-REPORT.md"
      provides: "Complete regression baseline documenting test results, route status, and known issues"
      contains: "## Test Suite Results"
  key_links:
    - from: "railway_apps/aiaa_dashboard/test_app.py"
      to: "railway_apps/aiaa_dashboard/app.py"
      via: "direct import and test_client()"
      pattern: "import app as flask_app"
---

<objective>
Run the existing test suite and audit every route in the AIAA Dashboard to establish a regression baseline before any hardening work begins.

Purpose: Phase 1 gates all subsequent phases (2-9). Without a clean baseline confirming what works today, any future regression is undetectable. This is observation only -- no code changes.

Output: `.planning/phases/01-regression-baseline/BASELINE-REPORT.md` -- a comprehensive Markdown report documenting test results, route-by-route HTTP status, API endpoint JSON validity, and any pre-existing issues.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@railway_apps/aiaa_dashboard/test_app.py
@railway_apps/aiaa_dashboard/app.py
@railway_apps/aiaa_dashboard/routes/views.py
@railway_apps/aiaa_dashboard/routes/api_v2.py
@railway_apps/aiaa_dashboard/routes/api.py
@railway_apps/aiaa_dashboard/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing test suite and audit all routes</name>
  <files>
    (no files modified -- observation only)
  </files>
  <action>
    Run the existing test suite and collect comprehensive route audit data. This task produces raw data that Task 2 compiles into the report.

    **Step 1: Run test_app.py**
    ```bash
    cd railway_apps/aiaa_dashboard && python3 test_app.py
    ```
    Capture full output including pass/fail status for all 7 tests. The tests must pass AS-IS with zero modifications.

    **Step 2: Write a temporary audit script**
    Create a temporary Python script (in `.tmp/` or inline) that uses the Flask test client to:

    A) Hit every VIEW route while authenticated and record HTTP status codes:
    - Public (no auth): `/health`, `/login` (GET), `/setup` (GET), `/onboarding`, `/api/health`
    - Protected (need session login first): `/`, `/home`, `/workflows`, `/executions`, `/env`, `/events`, `/settings/api-keys`, `/settings`, `/skills`, `/outputs`, `/clients-manage`, `/help`
    - Parameterized routes (test with dummy IDs -- expect 404 for missing resources, which is correct behavior): `/skills/nonexistent-skill/run`, `/executions/fake-id/progress`, `/executions/fake-id/output`

    B) Hit every API v2 GET endpoint and verify response is valid JSON with a `status` field:
    - Public: `/api/v2/skills`, `/api/v2/skills/search?q=test`, `/api/v2/skills/categories`, `/api/v2/skills/nonexistent-skill` (expect 404)
    - Protected (need session): `/api/v2/executions`, `/api/v2/executions/stats`, `/api/v2/settings/api-keys/status`, `/api/v2/settings/preferences`, `/api/v2/settings/profile`, `/api/v2/clients`

    C) Hit API v1 GET endpoints:
    - Public: `/api/health`
    - Protected: `/api/deployments`, `/api/workflows/deployable`

    For the audit script, authenticate by using `client.post('/login', data={'username': 'testadmin', 'password': ''}, follow_redirects=True)` (matching the test_app.py pattern with empty password hash).

    Record for each route: path, method, expected auth requirement, HTTP status code, whether response is valid (HTML for views, JSON for APIs), and any errors encountered.

    **Step 3: Note any console/import warnings**
    During both test_app.py and the audit script, capture any Python warnings, deprecation notices, or import issues that appear in stderr.

    **Important:**
    - Do NOT modify any application code
    - Do NOT modify test_app.py
    - The audit script is disposable -- delete it after collecting data
    - If a route returns 500, note it as a pre-existing issue but do not fix it
  </action>
  <verify>
    - `python3 railway_apps/aiaa_dashboard/test_app.py` exits with code 0 (all 7 tests pass)
    - Audit script runs to completion and produces route status data for all routes listed above
  </verify>
  <done>All 7 tests pass. Route audit data collected for every view route, API v1 endpoint, and API v2 endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Compile baseline report</name>
  <files>.planning/phases/01-regression-baseline/BASELINE-REPORT.md</files>
  <action>
    Using the data collected in Task 1, create `.planning/phases/01-regression-baseline/BASELINE-REPORT.md` with the following structure:

    ```markdown
    # Regression Baseline Report

    **Date:** [today's date]
    **App Version:** 5.0
    **Test Runner:** test_app.py (7 integration tests)

    ## Summary

    | Category | Total | Pass | Fail | Notes |
    |----------|-------|------|------|-------|
    | Existing Tests | 7 | ? | ? | |
    | View Routes (auth) | ? | ? | ? | |
    | View Routes (public) | ? | ? | ? | |
    | API v1 Endpoints | ? | ? | ? | |
    | API v2 Endpoints | ? | ? | ? | |

    ## Test Suite Results

    [List each of the 7 tests with PASS/FAIL status]

    ## View Routes Audit

    ### Public Routes (no auth required)

    | Route | Method | Expected Status | Actual Status | Result |
    |-------|--------|-----------------|---------------|--------|
    | /health | GET | 200 | ? | ? |
    | /login | GET | 200 | ? | ? |
    | ... | ... | ... | ... | ... |

    ### Protected Routes (auth required)

    | Route | Method | Expected Status | Actual Status | Result |
    |-------|--------|-----------------|---------------|--------|
    | / | GET | 302 (redirect to /home) | ? | ? |
    | /home | GET | 200 | ? | ? |
    | ... | ... | ... | ... | ... |

    ### Parameterized Routes (expected 404 for missing resources)

    | Route | Method | Expected Status | Actual Status | Result |
    |-------|--------|-----------------|---------------|--------|
    | /skills/nonexistent/run | GET | 404 | ? | ? |
    | ... | ... | ... | ... | ... |

    ## API v1 Endpoints Audit

    | Endpoint | Method | Auth | Expected Status | Actual Status | Valid JSON | Result |
    |----------|--------|------|-----------------|---------------|------------|--------|
    | /api/health | GET | No | 200 | ? | ? | ? |
    | ... | ... | ... | ... | ... | ... | ... |

    ## API v2 Endpoints Audit

    | Endpoint | Method | Auth | Expected Status | Actual Status | Valid JSON | Has status field | Result |
    |----------|--------|------|-----------------|---------------|------------|-----------------|--------|
    | /api/v2/skills | GET | No | 200 | ? | ? | ? | ? |
    | ... | ... | ... | ... | ... | ... | ... | ... |

    ## Pre-Existing Issues

    [List any routes that returned unexpected status codes, any Python warnings/deprecation notices, any import errors, any 500 responses]

    If none: "No pre-existing issues found."

    ## Deprecation Warnings

    [List any deprecation warnings from Python or Flask. These are acceptable and documented per CONTEXT.md decisions.]

    If none: "No deprecation warnings observed."

    ## Conclusion

    [One paragraph: Does the app pass baseline? Are all tests green? Are there blocking issues?]
    ```

    Fill in ALL cells with actual data from Task 1. Use PASS/FAIL markers. Do not leave any `?` placeholders.

    **Report criteria from CONTEXT.md:**
    - Existing tests must pass as-is (blocking)
    - Console JS errors noted but don't block (not applicable in server-side testing, but note if any client-side issues are inferred from template rendering errors)
    - Deprecation warnings documented but acceptable
  </action>
  <verify>
    - File exists at `.planning/phases/01-regression-baseline/BASELINE-REPORT.md`
    - File contains all sections: Summary, Test Suite Results, View Routes Audit, API v1 Audit, API v2 Audit, Pre-Existing Issues, Conclusion
    - Every route from views.py, api.py, and api_v2.py appears in the report
    - No placeholder `?` values remain -- all cells have actual test data
  </verify>
  <done>BASELINE-REPORT.md exists with complete data for all tests and routes. Every row has actual status codes, no placeholders remain. Summary section gives clear pass/fail verdict.</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. `python3 railway_apps/aiaa_dashboard/test_app.py` exits 0 -- all 7 tests pass
2. `.planning/phases/01-regression-baseline/BASELINE-REPORT.md` exists and is complete
3. Report covers all routes from views.py (21 routes), api.py (8 endpoints), api_v2.py (17 endpoints)
4. No application code was modified during this phase
</verification>

<success_criteria>
- All 7 existing tests pass without any modifications to test_app.py or application code
- Baseline report documents HTTP status for every route in the application
- Baseline report documents JSON validity for every API endpoint
- Any pre-existing issues are documented (not fixed)
- Phase 1 requirements TEST-01 and TEST-04 are addressed
</success_criteria>

<output>
After completion, create `.planning/phases/01-regression-baseline/01-01-SUMMARY.md`
</output>
