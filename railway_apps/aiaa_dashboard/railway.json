{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -lc 'WORKERS=1; if [ -n \"$REDIS_URL\" ]; then WORKERS=2; fi; exec gunicorn app:app --bind 0.0.0.0:$PORT --workers \"$WORKERS\" --timeout 120'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
