# nixpacks.toml for write_cold_emails.py
# Generated by example_minimal_docker.py
# Deploy: railway up

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install openai python-dotenv requests"]

[start]
cmd = "python execution/write_cold_emails.py"

# Required environment variables (set in Railway dashboard):
# OPENAI_API_KEY
# OPENROUTER_API_KEY
# PERPLEXITY_API_KEY
